// import axios from "axios";

// const DEFAULT_BASE = 'http://localhost:8080/api/arise';
// const API_BASE = import.meta.env.VITE_API_BASE_URL || DEFAULT_BASE;

// const api = axios.create({
// 	baseURL: API_BASE,
// 	headers: { 'Content-Type': 'application/json' }
// });

// export const listemp = () => api.get('/');
// export const getEmployee = (id) => api.get(`/${id}`);
// export const createEmployee = (payload) => api.post('/', payload);
// export const updateEmployee = (id, payload) => api.put(`/${id}`, payload);
// export const deleteEmployee = (id) => api.delete(`/${id}`);

// export default api;